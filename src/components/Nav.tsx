import React from 'react';
import styled from "styled-components";

const Nav = () => {
    return (
        <Container>

        </Container>
    );
};

const Container = styled.div`
  width: 70%;
  height: 100vh;
  left:0;
  z-index: 99999;
`

export default Nav;
